const config = {
    port: 8080,
    origins: ["https://repl-mail.mreconomical.repl.co", "https://repl-mail--mreconomical.repl.co"],
    reqByteLimit: 100000,

    database: "https://repl-mail-database.mreconomical.repl.co"
}

module.exports = {
    config: config
}